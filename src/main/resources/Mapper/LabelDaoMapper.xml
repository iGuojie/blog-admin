<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yu.blog.dao.LabelDao">

    <select id="selectLabelList" resultType="com.yu.blog.vo.LabelVO">
        SELECT
            l.id AS id,
            l.name AS name,
            l.descirption AS descirption,
            COALESCE(COUNT(lb.blog_id), 0) AS count
        FROM label l
        LEFT JOIN label_blogs lb ON l.id = lb.label_id
        GROUP BY l.id, l.name, l.descirption
        ORDER BY l.id
    </select>

    <select id="countBlogsByLabelId" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM label_blogs
        WHERE label_id = #{labelId}
    </select>

</mapper>
